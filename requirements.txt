# Everything a laptop or CI needs: the service, then the scripts, the evals and
# the dev tools. The Docker image installs only requirements-service.txt, so
# nothing below the first line ships.
-r requirements-service.txt

# Pinned exactly. These SDKs renamed methods between minor versions during
# preview - `>=` let pip pull a version whose API did not match the code.
# The service no longer uses any of them (docs/decisions/007). What still does:
#   azure-ai-agents          agents/deploy_agents.py
#   azure-search-documents   scripts/ingest.py
#   openai                   scripts/ingest.py, scripts/generate_bulletins.py
azure-ai-agents==1.1.0
azure-search-documents==12.0.0
openai==2.48.0

# Writing the bulletin PDFs (generate_bulletins.py) and reading them (ingest.py).
pymupdf==1.26.5
reportlab==5.0.1

# dev
pytest==8.4.2
ruff==0.16.7
